{
  "name": "bingo-app",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute bingo-app-db --remote --file=./schema.sql && wrangler d1 execute bingo-app-db --remote --file=./migrations/002_add_name.sql || true"
  },
  "devDependencies": {
    "wrangler": "^4.105.0"
  }
}
